using UnityEngine;

// Script will be used for creating lanes
// Since we are doing different lane environment for each lane must create lanes
public class LaneBuilder
{
    
    //Want to load lane 1 -> lane 2 -> etc
    public void CreateLane(CardDropArea area)
    {
        
        // build CardDropAreas here
        
    }
}
